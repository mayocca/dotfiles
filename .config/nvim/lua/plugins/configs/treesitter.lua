local options = {
  ensure_installed = { "c", "lua", "vim", "vimdoc", "query" },

  highlight = {
    enable = true,
    use_languagetree = true,
  },

  indent = { enable = true },
}

return options
